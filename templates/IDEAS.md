# Ideas — raw-idea staging tier (TEMPLATE)

A free-form scratchpad for raw ideas, observations, and concepts — no required structure. The earliest,
lowest-commitment staging tier: one step before `LEARNINGS.md` (which stages *workflow* insights) and
before `BACKLOG.md` (which holds only *actionable* items). An idea lands here so it isn't lost to the
chat transcript, but stays out of the always-loaded surfaces and out of the backlog until it actually
earns a next step.

- **On demand** — NOT auto-loaded (pointer in the `CLAUDE.md` map).
- **Format:** `- [date] idea — context (where it came from)`. Full freedom — write however helps you think.
- **Review:** at `/wrap` or a periodic review — promote to `BACKLOG.md` (once actionable) or `LEARNINGS.md`
  (if it's actually a workflow insight), or leave it here, or explicitly drop it.
- **Gate: Ideas != Backlog.** Backlog is actionable — a next step exists. Ideas are raw — a next step may
  never materialize, and that's fine; don't force a real idea into a premature ticket just to satisfy a
  red-flag sweep.

## Ideas

<!-- - [YYYY-MM-DD] **<idea>** — <context / where it came from>. -->
